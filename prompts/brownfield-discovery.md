This is a brownfield discovery pass. Read `AGENTS.md`,
`docs/spec-first-workflow.md`, and `docs/specs/README.md` first. Record which
product contracts are absent or unreliable before inspecting implementation.

Do not change product implementation. Inspect routes, state, code, tests, docs,
and UI flows only as evidence. Produce a product map, an authority/conflict
summary, a prioritized spec backlog, and short first-pass specs with explicit
unknowns. Classify historical or completed-migration materials so they cannot
silently compete with active contracts.
